#include "main.h"
#include <string.h>

int _isdigit(int c)
{
    /* Empty implementation for _isdigit */
    return (0);
}

int _strlen(char *s)
{
    /* Empty implementation for _strlen */
    return (0);
}

void _puts(char *s)
{
    /* Empty implementation for _puts */
}

char *_strcpy(char *dest, char *src)
{
    /* Empty implementation for _strcpy */
    return (NULL);
}

int _atoi(char *s)
{
    /* Empty implementation for _atoi */
    return (0);
}
