/**
 * mul - function to find the product of 2 numbers
 * @a: the int to be multiplied
 * @b: the other int
 * Return: product of @a and @b
 */

int mul(int a, int b)
{
	int product;
	product = a * b;
	return (product);
}
