#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *_strcpy - check the code
 *
 *@dest: a pointer variable
 *@src: another pointer variable
 *
 *Return: a pointer
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
